// Este arquivo é para armazenar a chave da API localmente durante o desenvolvimento.
// NÃO deve ser enviado para o repositório público.
// Para produção, use variáveis de ambiente no Render.com

const config = {
  OPENWEATHER_API_KEY: 'defa867a2d99fe9988da479d13f4ba9'
};

export default config;
